{
  "name": "@amulet-laboratories/rig-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "build:ci": "cd ../../Hex && pnpm install --frozen-lockfile && pnpm build && cd ../Rig/demo && pnpm install --frozen-lockfile && pnpm build",
    "preview": "vite preview",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "generate:health": "node scripts/generate-health-data.mjs"
  },
  "dependencies": {
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@floating-ui/vue": "^1.1.11",
    "@iconify/vue": "^4.3.0",
    "@playwright/test": "^1.58.2",
    "@vitejs/plugin-vue": "^6.0.4",
    "typescript": "^5.7.0",
    "vite": "^7.3.1",
    "vue-tsc": "^2.2.0"
  }
}
